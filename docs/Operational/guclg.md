# Change username/email for local repo only

```
git config --local user.name  "amdukf"
git config --local user.email "amdukf@gmail.com"
```

# To see lists in local and global

```
git config --list --local
git config --list --global
```