services:
  mkdocs:
    image: git.ecorp.kalekabab.ir/amdukf/welkinops:latest
    container_name: welkinops-docs
    restart: unless-stopped
    ports:
      - "127.0.0.1:8006:80"

volumes:
  welkinops-config:
    name: welkinops-config
    external: false
